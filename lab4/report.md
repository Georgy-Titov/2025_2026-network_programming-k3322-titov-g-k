# Отчет по лабораторной работе №4 - "Базовая 'коммутация' и туннелирование используя язык программирования P4".

## Шапка отчета

* University: [ITMO University](https://itmo.ru/ru/)
* Faculty: [ФПиН](https://fpin.itmo.ru/ru)
* Course: [Network-programming](https://itmo-ict-faculty.github.io/network-programming/)
* Year: 2025/2026
* Group: K3322
* Author: Titov Georgy Konstantinovich
* Lab: Lab2
* Date of create: 04.06.2026
* Date of finished: 06.06.2026

## Описание

В данной лабораторной работе вы познакомитесь на практике с языком программирования P4, разработанный компанией Barefoot (ныне Intel) для организации процесса обработки сетевого трафика на скорости чипа. Barefoot разработал несколько FPGA чипов для обработки трафика которые были встроенны в некоторые модели коммутаторов Arista и Brocade.

## Цель работы

Изучить синтаксис языка программирования P4 и выполнить 2 задания обучающих задания от Open network foundation для ознакомления на практике с P4.

## Задание

Ход работы:

1. склонировать репозиторий p4lang/tutorials;
2. уcтановить Vagrant и VirtualBox;
3. развернуть тестовую среду vagrant up;
4. выполнить задание [Implementing Basic Forwarding](https://github.com/p4lang/tutorials/tree/master/exercises/basic);
5. выполнить задание [Implementing Basic Tunneling](https://github.com/p4lang/tutorials/tree/master/exercises/basic_tunnel)

## Выполнение работы

### Устанвока Vagrant

Для подготовки среды разработки были установлены VirtualBox и Vagrant. VirtualBox использовался в качестве гипервизора для запуска виртуальной машины с операционной системой Ubuntu, а Vagrant — для автоматизации процесса её развертывания и настройки.

После установки необходимого программного обеспечения был клонирован репозиторий p4lang/tutorials, содержащий готовую конфигурацию виртуальной машины и набор учебных заданий по языку программирования P4:

```
git clone https://github.com/p4lang/tutorials.git
```

Далее был выполнен переход в каталог с конфигурацией виртуальной машины:

```
cd tutorials/vm-ubuntu-20.04
```

Запуск виртуальной машины осуществлялся командой:

```
vagrant up
```

> На данном этапе возникла проблема, связанная с ограничением доступа к необходимому образу Ubuntu из Российской Федерации. Для её решения базовый образ был загружен вручную, после чего процесс развертывания был успешно продолжен.

При первом запуске Vagrant автоматически создавал виртуальную машину в VirtualBox и выполнял её первоначальную настройку (provisioning), включая установку всех необходимых компонентов для работы с P4. В среду были установлены Mininet, программный коммутатор BMv2, компилятор p4c, библиотеки P4Runtime и сопутствующие инструменты.

После завершения настройки подключение к виртуальной машине выполнялось с помощью команды (Не обязательно подкючаться через консоль, VM поднимется сама и можно будет работать через GUI):

```
vagrant ssh  # если хотии работать через консоль
```

<img width="1558" height="844" alt="image" src="https://github.com/user-attachments/assets/a793ce7a-e96f-4283-af2f-2a54df6c9184" />

### Implementing Basic Forwarding

> В первом задании использовалась сеть, состоящая из нескольких хостов и программируемых коммутаторов BMv2. Целью работы являлась реализация базовой IPv4-маршрутизации средствами языка P4 и последующая проверка передачи пакетов между узлами сети.

Для запуска лабораторной среды использовалась команда:

```
make run
```

При первой попытке запуска возникла ошибка компиляции P4-программы. Причиной оказался формат файла P4Info. В используемой версии инструментов расширение .txtpb обрабатывалось некорректно, поэтому потребовалось заменить путь к файлу P4Info в utils/Makefile с:

```
build/basic.p4.p4info.txtpb

на

build/basic.p4.p4info.txt
```
Аналогичные изменения были внесены в файлы конфигурации P4Runtime (`s1-runtime.json`, `s2-runtime.json`, `s3-runtime.json` и другие).

После исправления конфигурации стенд успешно запускался, однако передача пакетов между узлами отсутствовала, поскольку логика маршрутизации в программе ещё не была реализована.

<img width="350" height="148" alt="image" src="https://github.com/user-attachments/assets/d4558758-dbda-4d5a-9fdf-d0b1debc480b" />

#### Структура программы P4

Программа на P4 состоит из нескольких основных этапов обработки:

* Parser — извлечение заголовков из входящего пакета;
* VerifyChecksum — проверка контрольных сумм;
* Ingress — основная логика обработки и маршрутизации;
* Egress — дополнительная обработка перед отправкой;
* ComputeChecksum — пересчёт контрольных сумм;
* Deparser — формирование выходного пакета.

#### Описание используемых заголовков

Для работы программы были определены структуры Ethernet и IPv4.

Ethernet-заголовок содержит:

* MAC-адрес назначения;
* MAC-адрес источника;
* поле EtherType.

```
header ethernet_t {
    macAddr_t dstAddr;
    macAddr_t srcAddr;
    bit<16> etherType; # Поле EtherType используется для определения типа инкапсулированного протокола. IPv4-заголовок содержит стандартный набор полей IPv4:
}
```
```
header ipv4_t {
    ...
    ip4Addr_t srcAddr;
    ip4Addr_t dstAddr; # Особый интерес для маршрутизации представляет поле dstAddr, по которому принимается решение о дальнейшей пересылке пакета.
}
```

#### Реализация парсера

Первым этапом обработки является извлечение заголовков из входящего пакета.

```
state start {
    packet.extract(hdr.ethernet);

    transition select(hdr.ethernet.etherType) {
        TYPE_IPV4: parse_ipv4;
        default: accept;
    }
}
```

Сначала коммутатор извлекает Ethernet-заголовок. Далее анализируется поле EtherType.

Если значение соответствует IPv4 (0x0800), выполняется переход к состоянию parse_ipv4.

```
state parse_ipv4 {
    packet.extract(hdr.ipv4);
    transition accept;
}
```

На этом этапе из пакета извлекается IPv4-заголовок, после чего управление передаётся в блок Ingress. Таким образом, после завершения работы парсера коммутатор получает доступ ко всем полям Ethernet и IPv4, необходимым для дальнейшей обработки.

#### Реализация логики маршрутизации

Основная логика программы реализована в блоке `MyIngress`.

Для обработки пакетов определены два действия.

Первое действие используется для удаления пакетов:

```
action drop() {
    mark_to_drop(standard_metadata);
}
```

Если для пакета не найден маршрут, он будет отброшен.

Второе действие отвечает за пересылку IPv4-пакетов:

```
action ipv4_forward(macAddr_t dstAddr,
                    egressSpec_t port)
{
    standard_metadata.egress_spec = port;

    hdr.ethernet.dstAddr = dstAddr;

    hdr.ipv4.ttl = hdr.ipv4.ttl - 1;
}
```

Во время выполнения действия происходит:

* Выбор выходного порта (egress_spec).
* Замена MAC-адреса назначения.
* Уменьшение значения TTL на единицу.

Уменьшение TTL является обязательной операцией для любого маршрутизатора, поскольку предотвращает бесконечную циркуляцию пакетов в сети.

#### Таблица маршрутизации

Для принятия решения о пересылке используется таблица `ipv4_lpm`.

```
table ipv4_lpm {
    key = {
        hdr.ipv4.dstAddr : lpm;
    }

    actions = {
        ipv4_forward;
        drop;
        NoAction;
    }

    size = 1024;
    default_action = drop();
}
```

В качестве ключа используется поле: `hdr.ipv4.dstAddr`

Тип поиска: `lpm` - означает Longest Prefix Match (поиск по самому длинному совпадающему префиксу).

Данный механизм является стандартным способом маршрутизации IP-пакетов и применяется в большинстве современных маршрутизаторов. Если для адреса найден маршрут, выполняется действие ipv4_forward. Если совпадение отсутствует, применяется действие по умолчанию drop().

#### Применение таблицы

Вызов таблицы выполняется в блоке `apply`.

```
apply {
    if (hdr.ipv4.isValid()) {
        ipv4_lpm.apply();
    }
}
```

#### Формирование выходного пакета

После завершения обработки пакет должен быть снова преобразован в последовательность байтов.

Для этого используется блок `Deparser`.

```
control MyDeparser(packet_out packet,
                   in headers hdr)
{
    apply {
        packet.emit(hdr.ethernet);
        packet.emit(hdr.ipv4);
    }
}
```

Сначала в пакет записывается Ethernet-заголовок, затем IPv4-заголовок. После этого пакет передаётся на выбранный выходной интерфейс.

#### Проверка работоспособности

После завершения реализации программы стенд был повторно запущен командой:

```
make run
```

Проверка связности выполнялась с помощью утилиты `pingall` между хостами Mininet.

<img width="425" height="186" alt="image" src="https://github.com/user-attachments/assets/4c7a83a4-d876-4a7f-a76a-c377df3abf59" />

Успешное прохождение ICMP Echo Request и ICMP Echo Reply подтвердило корректную работу.

## Вывод

В ходе выполнения лабораторной работы были изучены возможности интеграции системы управления сетевой инфраструктурой NetBox с системой автоматизации Ansible. Был развернут и настроен сервер NetBox, включающий PostgreSQL в качестве базы данных и Redis для обеспечения работы фоновых задач и кэширования. Для доступа к API был создан и настроен токен версии v2.

В рамках работы были разработаны и протестированы три Ansible-плейбука. Первый плейбук выполнял экспорт данных из NetBox и формировал локальный JSON-файл с информацией о площадках, устройствах, интерфейсах и IP-адресах. Второй плейбук осуществлял сбор уникального идентификатора MikroTik CHR (system-id) и автоматически записывал его в поле серийного номера соответствующего устройства в NetBox. Третий плейбук демонстрировал использование NetBox в качестве единого источника достоверных данных (Source of Truth): из NetBox автоматически получались параметры устройств, после чего выполнялась настройка маршрутизаторов, включая изменение имени устройства и назначение IP-адреса на интерфейс WireGuard.

В процессе выполнения работы были рассмотрены вопросы взаимодействия Ansible с API NetBox, использования коллекции netbox.netbox, работы с динамическими данными инвентаря, а также особенности автоматизации настройки устройств MikroTik RouterOS через SSH.

Полученные результаты подтвердили возможность централизованного хранения информации о сетевой инфраструктуре в NetBox и её последующего использования для автоматизированного управления сетевыми устройствами. Использование связки NetBox и Ansible позволяет существенно сократить объём ручных операций, повысить актуальность конфигурационных данных и снизить вероятность ошибок при администрировании сети.


